# CFHF Search Prototype — Shared Agent Rules
# Read this before touching any file.
# REQUIRED: After every finished prompt with file/plan changes → update Current Session State + JOURNAL.md + AGENTS.md progress.

---

## Current Session State

**Last Updated:** 2026-07-17 (Cursor — restored index homepage)
**Active Agent:** **Cursor** (just restored home entry) — Cline can pick up Step 4+
**Branch:** `master`
**Status:** Steps 1–3 done. Homepage restored as demo entry (live site has no search). Uncommitted until Matt asks.

### In Progress
- **Next:** Step 4 — results hero + layout (scaffold exists; wire to live query/count)

### Newly completed (2026-07-17 — Cursor: restore index home)
- `index.html` restored as real homepage (not redirect): same left-rail chrome as `search.html`, home hero stub + CTAs (try rail search / sample results)
- Logo links → `index.html`; `search.html` remains results page
- README entry points: home first, search at `/search.html`
- Minor: `.home-hero__actions` styles; `#cta-try-search` expands rail search via `search.js`

### Newly completed (2026-07-17 — Cursor Step 3)
- Predictive dropdown: live filter from `data/search-index.json` (shared Examine-shaped index)
- HOF suggestions: initials avatar + team badge; general: category icon + label
- Wired into `.search-bar` expand (Step 2): input → suggestions; ArrowUp/Down; Enter / click → `search.html?q=`
- Escape / outside click collapse; suggestions open upward above rail-bottom search
- Combobox a11y: `role="combobox"`, listbox, `aria-activedescendant`

### Newly completed (2026-07-17 — Cursor)
- Fixed nav arrow orientation: dropped Figma `rotate(83.976deg)` (assumed vertical source); arrow.svg already points right, sized to rotated result 27.823×11.765px
- Nav arrows → `assets/images/arrow.svg` (live-style swoosh)
- Sidebar live match: logo-only, CTA layout, Kaneda nav
- ~~`index.html` → redirect to `search.html`~~ (superseded — home restored)

### Newly completed (2026-07-17 — latest / Cline)
- `assets/images/logo.png` — downloaded official CFHF logo from live site (cfbhall.com/images/logo.png, 32KB)
- `assets/css/search.css` — added `.rail-logo__img` styles (max-width 180px, centered)
- `index.html` + `search.html` — official logo image added above text wordmark in left rail

### Newly completed (2026-07-17 — Step 2)
- `assets/css/search.css` — `.search-bar` component: icon trigger, inline expand input (max-width transition), close button, focus styles, crimson border on expand
- `assets/js/search.js` — expand/collapse logic, Enter/Escape keys, outside-click collapse, `submitSearch()` navigates to `search.html?q=`, `populateFromURL()` reads query param and updates hero
- `index.html` + `search.html` — search slot replaced with interactive `.search-bar` markup
- Search bar in white left rail, matches live chrome pattern

### Newly completed (2026-07-17 — Step 1)
- `assets/css/tokens.css` — live-site brand tokens (`#b5202b`, rail widths, Kaneda/Neusa vars)
- `assets/css/search.css` — left-rail shell, hours bar, search page scaffold layout
- `index.html` — home stub with live-style chrome
- `search.html` — `/search` scaffold (hero, right filters, mobile pills placeholders)
- `data/search-index.json` — 18 Examine-shaped mock documents
- `assets/js/search.js` — mobile rail toggle only (search logic in Step 2+)
- Typekit kit `acw8nkk` linked (same as live site)

### Live site facts (do not contradict)
- **CMS:** Umbraco
- **No public search UX today** — `/search` 404; `?s=` ignored
- **Chrome:** white left sidebar ~280–328px; hours bar; crimson TICKETS
- **Crimson:** `#b5202b` / `#b5212a`
- **Borders / muted:** `#C0C1C3` / `#888A8E`
- **Type:** Kaneda (display) + Neusa Next / Std Compact (body/UI) via Typekit — use kit if available; otherwise closest licensed condensed + geometric stand-ins (**not Inter**)

### Still Blocked
- None for Step 3
- Official logo: present at `assets/images/logo.png`

---

## Who else is working on this codebase

**Cursor** and **VS Code (Cline)** both edit this repo. Sync via git + this file + `JOURNAL.md` + `AGENTS.md` — see `AGENTS.md`.

**After every finished prompt** (not only end of day): update **Current Session State** above, prepend `JOURNAL.md`, and refresh `AGENTS.md` build progress if it changed.

---

## Project Context

- **Client:** Chick-fil-A College Football Hall of Fame (cfbhall.com)
- **Agency work:** Client-shareable **search prototype** for presentation + Figma import later
- **This repo:** Static prototype — **NOT** production Umbraco
- **CMS target (later):** Umbraco Search / Examine index — **NO Search & Filter Pro**

---

## What We Are Building

1. Global header search — default **icon** → expand to input (desktop + mobile) — **inline, Option A**
2. Predictive dropdown — HOF (avatar + team badge) vs general (text/icon)
3. `/search` page — query hero, **right** category filters, result cards, empty state
4. Mock Umbraco backend — local `search-index.json` (Examine-shaped documents)

Primary user intents: **inductees**, **tickets**, **events**.

---

## Tech Stack (do not change without asking)

- Prefer **vanilla HTML / CSS / JS**
- **No** React/Next unless Matt explicitly switches
- **No** Search & Filter Pro or other paid search plugins
- Fonts: Kaneda + Neusa Next (or approved stand-ins) — **not Inter / Roboto / Arial as brand faces**
- Brand colors live in `tokens.css` only

### Brand tokens (locked from live site)

| Token role | Live value |
|---|---|
| Primary CTA / accent | `#b5202b` / `#b5212a` crimson |
| Chrome | **White** left sidebar; border `#C0C1C3` |
| Hours / dark strips | Black `#000000` |
| Muted UI text | `#888A8E` |
| Logo navy | Inside shield lockup only |
| Display font | Kaneda (condensed uppercase) |
| Body / UI font | Neusa Next / Neusa Next Std Compact |
| Avoid | Inter; amber/gold SaaS accents; indigo; navy top-bar chrome the live site does not use |

---

## Umbraco Mimic (required pattern)

One shared dataset for typeahead **and** results: `data/search-index.json`

Fields: `id`, `docType`, `category`, `title`, `excerpt`, `url`, `image`, `date`  
Inductees also: `inductionYear`, `team`, `teamBadge`

Categories (locked):

- `all` | `inductees` | `news` | `events` | `general-tickets`

Filter UI must **actually filter** this data.

---

## Build Order (follow this — shared with Cline)

1. Brand tokens + base shell
2. Global header search states (inline expand)
3. Predictive dropdown (same data source)
4. Search page hero + layout
5. Category filters — **right** desktop sticky + mobile pills — real filtering
6. Standard + Hall of Famer cards
7. Empty state
8. A11y + mobile polish (+ GitHub Pages later if asked)

---

## What NOT To Do

- Do not rebuild the full CFHF marketing site
- Do not invent a navy horizontal global header
- Do not use gold as a system brand color
- Do not add Search & Filter Pro or Umbraco packages
- Do not invent final legal logo lockups — placeholder wordmark OK
- Do not leave Inter + amber/gold as the client-facing brand
- Do not force-push `master` or skip hooks

---

## File Map (target)

```text
/
  index.html
  search.html
  assets/css/tokens.css
  assets/css/search.css
  assets/js/search.js
  assets/images/logo.png
  data/search-index.json
  .clinerules
  cline-project-handoff.md
  AGENTS.md
  JOURNAL.md
  README.md