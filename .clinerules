# CFHF Search Prototype — Shared Agent Rules
# Read this before touching any file.
# REQUIRED: After every finished prompt with file/plan changes → update Current Session State + JOURNAL.md + AGENTS.md progress.

---

## Current Session State

**Last Updated:** 2026-07-17 (Cursor — hours search height + expand left)
**Active Agent:** **Cursor**
**Branch:** `master`
**Status:** Hours-bar search: matched-height crimson square + expand-left on focus. Next: Step 4+.

### In Progress
- **Next:** Step 4 — results hero + layout (wire to live query/count)

### Newly completed (2026-07-17 — Cursor: hours search height + expand left)
- Icon square height matches input (1.75rem); input expands left on focus

### Newly completed (2026-07-17 — Cursor: crimson square search icon)
- Input always visible with “Search…” placeholder to the **left** of a **white** search icon
- Removed expand/collapse + close button from hours-bar search
- Files: `index.html`, `search.html`, `assets/css/search.css`, `assets/js/search.js`

### Newly completed (2026-07-17 — Cursor: hero text spacing)
- Vertically centered `.hero-slide` content (`justify-content: center`)
- Gaps: tight title lines; medium title→subtitle; tight subtitle→body; large body→CTA
- Carousel controls at bottom; hours-bar bottom rule on desktop
- Files: `assets/css/home.css`, `assets/css/search.css`

### Newly completed (2026-07-17 — Cline: hours-bar search)
- Search moved from hero overlay into the `.hours-bar` alongside "Important Details +"
- `.hours-bar__end` flex container: search icon → inline expand input → "Important Details +" link
- CSS: `.search-bar--hours` (transparent bg, white text, crimson icon, dark dropdown), `.hours-bar__end` layout
- Both `index.html` and `search.html` share identical hours-bar search markup
- Removed dead `.hero-carousel__search` CSS
- Rail remains logo/CTAs/nav only (no search)

### Newly completed (2026-07-17 — Cursor: live home sections on index)
- `index.html`: hero carousel (3 slides), Destination + cards + Happenings, Join the Legacy / Tebow quote
- `assets/css/home.css`, `assets/js/home.js` — carousel + section styles
- Rail remains logo/CTAs/nav only

### Newly completed (2026-07-17 — Cline: search UX refactor)
- Search removed from left rail on both `index.html` and `search.html`
- CSS: hero search styling (translucent white bg, white text, downward dropdown)
- JS: generalized from `.rail-search-slot` to `.search-bar-wrap`

### Newly completed (2026-07-17 — Step 3)
- Predictive dropdown: live filter from `data/search-index.json`
- HOF suggestions: initials avatar + team badge; general: category icon + label
- Combobox a11y: `role="combobox"`, listbox, `aria-activedescendant`

### Newly completed (2026-07-17 — Step 2)
- `.search-bar` component: icon trigger, inline expand input (max-width transition), close button
- `search.js`: expand/collapse, Enter/Escape, outside-click, `submitSearch()`, `populateFromURL()`

### Newly completed (2026-07-17 — Step 1)
- Brand tokens + left-rail shell + search page scaffold + 18-doc mock index
- Typekit kit `acw8nkk` linked

### Live site facts (do not contradict)
- **CMS:** Umbraco
- **No public search UX today** — `/search` 404; `?s=` ignored
- **Chrome:** white left sidebar ~280–328px; hours bar; crimson TICKETS
- **Crimson:** `#b5202b` / `#b5212a`
- **Borders / muted:** `#C0C1C3` / `#888A8E`
- **Type:** Kaneda (display) + Neusa Next / Std Compact (body/UI) via Typekit

### Still Blocked
- None

---

## Who else is working on this codebase

**Cursor** and **VS Code (Cline)** both edit this repo. Sync via git + this file + `JOURNAL.md` + `AGENTS.md`.

**After every finished prompt:** update **Current Session State**, prepend `JOURNAL.md`, refresh `AGENTS.md`.

---

## Project Context

- **Client:** Chick-fil-A College Football Hall of Fame (cfbhall.com)
- **Agency work:** Client-shareable **search prototype** for presentation + Figma import later
- **This repo:** Static prototype — **NOT** production Umbraco
- **CMS target (later):** Umbraco Search / Examine index — **NO Search & Filter Pro**

---

## What We Are Building

1. Global header search — default **icon** → expand to input (desktop + mobile) — **inline, Option A**
2. Predictive dropdown — HOF (avatar + team badge) vs general (text/icon)
3. `/search` page — query hero, **right** category filters, result cards, empty state
4. Mock Umbraco backend — local `search-index.json` (Examine-shaped documents)

---

## Tech Stack (do not change without asking)

- Prefer **vanilla HTML / CSS / JS**
- **No** React/Next, **No** Search & Filter Pro
- Fonts: Kaneda + Neusa Next — **not Inter / Roboto / Arial as brand faces**

### Brand tokens (locked from live site)

| Token role | Live value |
|---|---|
| Primary CTA / accent | `#b5202b` / `#b5212a` crimson |
| Chrome | **White** left sidebar; border `#C0C1C3` |
| Hours / dark strips | Black `#000000` |
| Muted UI text | `#888A8E` |
| Display font | Kaneda (condensed uppercase) |
| Body / UI font | Neusa Next / Neusa Next Std Compact |
| Avoid | Inter; amber/gold SaaS accents; indigo; navy top-bar |

---

## Umbraco Mimic (required pattern)

One shared dataset for typeahead **and** results: `data/search-index.json`

Fields: `id`, `docType`, `category`, `title`, `excerpt`, `url`, `image`, `date`  
Inductees also: `inductionYear`, `team`, `teamBadge`

Categories (locked): `all` | `inductees` | `news` | `events` | `general-tickets`

---

## Build Order

1. Brand tokens + base shell ✅
2. Global header search states ✅
3. Predictive dropdown ✅
4. Search page hero + layout (next)
5. Category filters (real filtering)
6. Standard + HOF cards
7. Empty state
8. A11y + mobile polish

---

## What NOT To Do

- Do not rebuild the full CFHF marketing site
- Do not invent a navy horizontal global header
- Do not use gold as a system brand color
- Do not add Search & Filter Pro or Umbraco packages
- Do not leave Inter + amber/gold as the client-facing brand
- Do not force-push `master` or skip hooks

---

## File Map (target)

```text
/
  index.html
  search.html
  assets/css/tokens.css
  assets/css/search.css
  assets/css/home.css
  assets/js/search.js
  assets/js/home.js
  assets/images/logo.png
  assets/images/arrow.svg
  data/search-index.json
  .clinerules
  cline-project-handoff.md
  AGENTS.md
  JOURNAL.md
  README.md