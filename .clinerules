# CFHF Search Prototype — Shared Agent Rules
# Read this before touching any file.
# REQUIRED: After every finished prompt with file/plan changes → update Current Session State + JOURNAL.md + AGENTS.md progress.

---

## Current Session State

**Last Updated:** 2026-07-17 (Grok — full agent continuity sync; client-ready)
**Active Agent:** **Grok**
**Branch:** `master` (synced with origin)
**Status:** Build Steps 1–8 **COMPLETE**. Client-shareable on GitHub Pages. Continuity docs refreshed.

### Live
- https://mattybotstew.github.io/cfhf-search-prototype/
- Pages source: `master` / root + `.nojekyll`

### Decision LOCKED (2026-07-17 — Matt)
Matt: “i want what is there now with the search functionality requested.”
- Chrome: always-visible stroked input + crimson square submit; expands left on hover/focus
- Functionality: live predictive dropdown (HOF portrait + team badge vs general icon/label)
- Do **not** switch to icon-only click-expand

### In Progress / Next
- Client feedback, design iteration, Figma capture as needed
- Optional later: real CMS portrait URLs in `image` field; production Umbraco API swap

### Newly completed (session summary — 2026-07-17)
- Steps 4–8: results hero, real filters, HOF/standard cards, empty state, a11y/mobile baseline
- Mobile: live-style topbar (hamburger · logo · TICKETS); hero layout match; typeahead z-index fix
- Search page: solid black hours bar; extra space under bar
- HOF fidelity: silhouette portraits, shield badges, Inducted year treatment
- Sticky filter sidebar + mobile pill tap targets + dynamic counts
- GitHub Pages enabled and verified (200s)

### Live site facts (do not contradict)
- **CMS:** Umbraco
- **No public search UX today** — `/search` 404; `?s=` ignored
- **Chrome:** white left sidebar ~280–328px; hours bar; crimson TICKETS
- **Crimson:** `#b5202b` / `#b5212a`
- **Borders / muted:** `#C0C1C3` / `#888A8E`
- **Type:** Kaneda (display) + Neusa Next / Std Compact (body/UI) via Typekit

### Still Blocked
- None

---

## Who else is working on this codebase

**Cursor**, **Cline (VS Code)**, and **Grok** edit this repo. Sync via git + this file + `JOURNAL.md` + `AGENTS.md`.

**After every finished prompt:** update **Current Session State**, prepend `JOURNAL.md`, refresh `AGENTS.md`.

---

## Project Context

- **Client:** Chick-fil-A College Football Hall of Fame (cfbhall.com)
- **Agency work:** Client-shareable **search prototype** for presentation + Figma import later
- **This repo:** Static prototype — **NOT** production Umbraco
- **CMS target (later):** Umbraco Search / Examine index — **NO Search & Filter Pro**

---

## What We Are Building

1. Global header search — hours bar: always-visible input + crimson square; focus expands left; typeahead
2. Predictive dropdown — HOF (portrait + team badge) vs general (text/icon)
3. `/search` page — query hero, **right** sticky category filters, result cards, empty state
4. Mock Umbraco backend — local `search-index.json` (Examine-shaped documents)

---

## Tech Stack (do not change without asking)

- Prefer **vanilla HTML / CSS / JS**
- **No** React/Next, **No** Search & Filter Pro
- Fonts: Kaneda + Neusa Next — **not Inter / Roboto / Arial as brand faces**

### Brand tokens (locked from live site)

| Token role | Live value |
|---|---|
| Primary CTA / accent | `#b5202b` / `#b5212a` crimson |
| Chrome | **White** left sidebar; border `#C0C1C3` |
| Hours / dark strips | Black `#000000` |
| Muted UI text | `#888A8E` |
| Display font | Kaneda (condensed uppercase) |
| Body / UI font | Neusa Next / Neusa Next Std Compact |
| Avoid | Inter; amber/gold SaaS accents; indigo; navy top-bar |

---

## Umbraco Mimic (required pattern)

One shared dataset for typeahead **and** results: `data/search-index.json`

Fields: `id`, `docType`, `category`, `title`, `excerpt`, `url`, `image`, `date`  
Inductees also: `inductionYear`, `team`, `teamBadge`

Empty `image` → prototype renders silhouette portrait (team colors). Real CMS images replace when set.

Categories (locked): `all` | `inductees` | `news` | `events` | `general-tickets`

---

## Build Order

1. Brand tokens + base shell ✅
2. Global header search states ✅
3. Predictive dropdown ✅
4. Search page hero + layout ✅
5. Category filters (real filtering) ✅
6. Standard + HOF cards ✅
7. Empty state ✅
8. A11y + mobile polish ✅

---

## What NOT To Do

- Do not rebuild the full CFHF marketing site
- Do not invent a navy horizontal global header
- Do not use gold as a system brand color
- Do not add Search & Filter Pro or Umbraco packages
- Do not leave Inter + amber/gold as the client-facing brand
- Do not force-push `master` or skip hooks
- Do not revert hours-bar search to icon-only Option A

---

## File Map (target)

```text
/
  index.html
  search.html
  assets/css/tokens.css
  assets/css/search.css
  assets/css/home.css
  assets/js/search.js
  assets/js/home.js
  assets/images/logo.png
  assets/images/arrow.svg
  data/search-index.json
  .nojekyll
  .clinerules
  cline-project-handoff.md
  AGENTS.md
  JOURNAL.md
  README.md
```
